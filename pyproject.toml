[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RET"]

[tool.ruff.format]
quote-style = "double"

[tool.pyright]
include = ["app.py", "src"]
