name: Keep Render Awake

on:
  schedule:
    - cron: '*/10 13-3 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS https://inventory-control-system-free.onrender.com > /dev/null
